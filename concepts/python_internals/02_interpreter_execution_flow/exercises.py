# Interpreter Execution Flow Exercises

import ast

# Exercise: Parse and dump AST for an expression

expr = "a + b * c"

# TODO: Use ast.parse and ast.dump
# Uncomment
# tree = ast.parse(expr)
# print(ast.dump(tree))