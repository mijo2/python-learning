# Late Binding Behavior Exercises

# Exercise: Fix late binding in a list comprehension

# TODO: Create a list of lambdas that capture loop variable correctly
# Uncomment
# funcs = [lambda x=i: x for i in range(3)]
# print([f() for f in funcs])  # Should be [0, 1, 2]