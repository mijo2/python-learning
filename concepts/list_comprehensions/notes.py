"""
LIST COMPREHENSIONS — NOTES

List comprehension is a concise way to create lists.

Basic syntax:

    new_list = [expression for item in iterable]

With condition:

    new_list = [expression for item in iterable if condition]

With nested loops:

    new_list = [expression for x in a for y in b]

Why use them:
- More readable than map/filter in many cases
- Usually faster than manual loops
- Declarative style
"""
